// search的小仓库
const state={};// 存储数据的地方
const mutations={};// 修改state的唯一手段
const actions={};// 用来写业务逻辑，也可以处理异步
const getters={};// 理解为计算属性，用于简化仓库数据，让组件获取仓库额数据更加方便

// 对外暴露
export default {
    state,mutations,actions,getters
}